<%= form_with model: trainer do |f| %>
    <% flash.each do |name, msg| -%>
        <%= content_tag :div, msg, class: name %>
    <% end %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit %>
<% end %>
